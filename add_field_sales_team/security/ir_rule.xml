<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate='0'>


    <!-- <record id="sale.sale_order_see_all" model="ir.rule">
        <field name="name">see his record and his team record and the records that dose not has sales person</field>
        <field ref="sale.model_sale_order" name="model_id"/>
        <field name="domain_force">['|',('team_id.user_id','=',user.id),('team_id.report_to_ids','in', user.id)]</field>
        <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman_all_leads'))]"/>
    </record> -->

    <record id="sale.sale_order_see_all" model="ir.rule">
        <field name="name">All Orders</field>
        <field ref="sale.model_sale_order" name="model_id"/>
        <field name="domain_force">['|',('team_id.user_id','=',user.id),('team_id.report_to_ids','in', user.id)]</field>
        <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman_all_leads'))]"/>
    </record>

    <record id="crm.crm_rule_all_lead" model="ir.rule">
        <field name="name">All Leads</field>
        <field ref="crm.model_crm_lead" name="model_id"/>
        <field name="domain_force">['|',('team_id.user_id','=',user.id),('team_id.report_to_ids','in', user.id)]</field>
        <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman_all_leads'))]"/>
    </record>

    

</odoo>
    